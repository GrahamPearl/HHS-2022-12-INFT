program testsnub1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

begin
  try
    { TODO -oUser -cConsole Main : Insert code here }
    WriteLN('Loading');
    WriteLN('Closing');
     ReadLN;

  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
